import { companyQueryKeys } from './company-keys';
import { invoiceQueryKeys } from './invoice-keys';

export const QueryKeys = {
  company: companyQueryKeys,
  invoice: invoiceQueryKeys,
};

export default QueryKeys;
